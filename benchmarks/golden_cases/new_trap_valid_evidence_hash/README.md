# new_trap_valid_evidence_hash

The provider output includes a `NEW` finding whose citation is bound by a valid
`evidence_hash`. The benchmark should count the new trap as precise and the
citation as valid.
