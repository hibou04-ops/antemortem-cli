# wrong_evidence_snippet

The citation points at an existing source line, but the supplied
`evidence_snippet` quotes different text from the same file. The case verifies
that the benchmark treats evidence-bound citation drift as invalid, not merely
line-bound.
